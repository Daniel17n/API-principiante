const Laptop = require('../models/laptopModel');

function getModel(req, res) {
  res.send('De momento no hay modelos disponibles');
}

function getDB(req, res) {
  res.send('Ostras! Se nos ha olvidado encender la base de datos, espera... [F5]');
}

function addLaptop(req, res) {
  const laptop = new Laptop(req.body);

  laptop.save((error, newLaptop) => {
    if (error) return res.status(400).send({ message: 'Error saving this laptop', error });

    return res.status(200).send({ message: 'Saved laptop', newLaptop });
  });
}

/* function findLaptop(req, res) {
  res.send('Ostras! Se nos ha olvidado encender la base de datos, espera... [F5]');
} */

module.exports = {
  getDB,
  getModel,
  addLaptop,
  // findLaptop,
};
