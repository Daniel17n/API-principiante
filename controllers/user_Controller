function getUsers(req, res) {
  res.json({
    message: 'No hay usuarios, de hecho, no hay nada.',
  });
}

function getDB(req, res) {
  res.json({
    message: 'Ostrás! Se nos ha olvidado la base de datos en casa :/',
  });
}

module.exports = {
  getDB,
  getUsers,
};
